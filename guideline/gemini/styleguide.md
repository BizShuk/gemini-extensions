
# Convension

## Golang

- Use `Screaming Snake Case` for constant variable, like "CONSTANT_CASE".
- Use `snake Case` for file name if single term can not be represent the meanning
- Use `Camel Case` for function/method/struct
- indent space is 4 spaces

## Pure Note(ignore)

B. .gemini/styleguide.md (風格指南)
這專注於「美觀」與「語法一致性」。
用途：定義縮排、註解格式、是否使用特定的 Linter 規則。
範例：使用 CamelCase 命名、Go 註解必須以函數名稱開頭。
